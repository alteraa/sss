# Python 3.12+ recommended.
# `aec-audio-processing` requires Python >= 3.11.
# For Silero VAD, install matching CPU wheels for `torch` and `torchaudio`.

# Core project dependencies
numpy
openai
python-dotenv
torch
torchaudio

# Target realtime audio stack
sounddevice
aec-audio-processing

# Optional extras intentionally omitted from default install:
# - `pyrnnoise`: optional second-stage noise suppression experiments
# - `soundfile`: optional file-based audio debug/export utilities
