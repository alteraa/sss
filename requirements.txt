# Python 3.12+ recommended.
# `aec-audio-processing` requires Python >= 3.11.
# For Silero VAD, install matching CPU wheels for `torch` and `torchaudio`.

# Core project dependencies
numpy
openai
python-dotenv
torch
torchaudio

# Target realtime audio stack
sounddevice
soundfile
aec-audio-processing
pyrnnoise
